# 012 — 白蓝桌面聊天与提示词生效

用户于 2026-09-06 授权自主执行，按下面顺序实施，2026-09-07 10:00 Asia/Shanghai 停止。当前自动跟进 ID：`automation`，每五分钟检查额度，不使用重置卡。

## 合同

1. 根据用户 2026-09-07 的最终修正，全局恢复原有宣纸暖色、朱砂与金色设计；非聊天页面不改变视觉风格。聊天页保留第 2 条所述的新交互与布局，并沿用全局原有色彩令牌。
2. 聊天采用 Codex 的安静单列布局：简洁会话目录、留白的助手正文、靠右浅色用户气泡、底部圆角多行输入容器和右下角发送/停止。保留头像档案入口、群聊身份、@ 补全、历史分页、流式取消及错误恢复。根据用户最终修正，输入框不提供模型切换，始终使用每位道人自身配置的模型。
3. 修复金丹与人设不生效：沿有效能力快照、语言模式合成、对话提示词及实际 LLM 消息检查。明确人设决定身份与语气，启用金丹决定能力与方法；禁用/零权重效果不得注入。测试覆盖组合生效与无金丹降级。
4. 仅清理确认冗余的代码，保护用户原有工作。中英文文案同步。
5. 功能测试、Go/Python 回归、前端 lint/typecheck/build；Wails 桌面实际行为作为 UI 验收。无法执行的环境或平台测试明确记录，不能冒充通过。

## 边界

仅维护 Wails 桌面产品，macOS arm64/x64、Windows x64。不得新增 Web 部署能力。实现保存在分支 `012-codex-chat-refresh` 的独立工作树，根目录已有未提交产物和用户文件保持不动。不自动推送、合并、发布。
